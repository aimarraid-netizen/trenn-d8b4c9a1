[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E4", "E7", "E9", "F", "I"]

[tool.ruff.lint.isort]
known-first-party = ["db", "queries", "analyze", "exercise_config", "hr_config", "validation"]

[tool.pytest.ini_options]
testpaths = ["tests"]
